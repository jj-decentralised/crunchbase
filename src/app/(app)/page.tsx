import { Card, CardContent } from "@/components/ui/card";

export default function OverviewPage() {
  return (
    <Card>
      <CardContent className="py-10 text-center text-sm text-muted">
        Overview dashboard — coming up next.
      </CardContent>
    </Card>
  );
}
